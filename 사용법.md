# MTPE 번역 파이프라인 — 사용법

작품·언어별 **5단계 프롬프트 체인**으로 MT(기계번역)를 추출하는 도구.
번역 결과의 사람 검수(포스트에디팅)는 CAT 등 기존 도구에서 진행합니다 (이 도구는 **MT 추출 엔진**에 집중).

---

## 0. 실행

- **앱으로 (가장 쉬움)**: Finder에서 **`MTPE.app` 더블클릭** → 네이티브 창으로 UI가 열림. (Dock에 두면 편함)
- **서버로**: 터미널에서
  ```bash
  cd "…/claude code"
  ./.venv/bin/python -m mtpe.server      # 브라우저에서 http://localhost:8000
  ```
- 창을 닫으면 서버도 종료됩니다.

화면은 상단 **[번역]** / **[프롬프트 관리]** 두 탭으로 나뉩니다.

---

## 1. 처음 한 번만: API 키 등록

1. **[번역]** 탭 → `1. 모델 & API 키`
2. 사용할 **모델** 선택 (예: Claude Opus 4.8)
3. 그 모델에 맞는 **API 키**를 입력하고 **[저장]**
   - 키는 이 컴퓨터의 `.env` 파일에만 저장됩니다(외부 전송 없음).
   - 한 번 저장하면 다음부터 자동 사용.

> **키 없이 먼저 써보려면**: 모델에서 **`🧪 키 없이 테스트 (모의 응답)`** 선택. 실제 호출 없이 흐름만 확인할 수 있습니다(결과는 자리표시자).

---

## 2. 번역하기 (운영자)

**[번역]** 탭에서 위에서 아래로 채우면 됩니다.

1. **모델** — 위에서 키 저장한 모델 선택 (단계별로 다른 모델을 쓰려면 프롬프트 관리에서 지정)
2. **작품 / 언어 / 버전** — 번역할 작품과 언어, 프롬프트 버전(기본 최신)
3. **설정집(TB)** — `업로드/교체`로 엑셀(.xlsx) 업로드 (작품당 1개 유지)
   - **AI에 보낼 시트 선택**: 업로드한 TB의 시트 중 체크한 것만 AI에 전달
   - **양식이 필요하면** `📄 양식 템플릿 내려받기`로 받아서 작성
4. **회차 원문** — 두 가지 방법 (둘 다 가능)
   - **폴더 경로 지정**: 회차 파일들이 있는 폴더 경로 입력 → `적용`
   - **개별 업로드**: 파일 선택 → `회차 추가`
   - 지원 형식: txt / md / **docx**
5. **회차 선택** — 번역할 회차를 체크 (`전체`/`해제`). 업로드한 회차는 ✕로 삭제 가능
6. **▶ 선택 회차 번역** 클릭

→ 오른쪽 **진행 상황**에 회차별로 STEP1~5 진행이 실시간 표시되고,
   회차마다 **최종 결과**가 나옵니다. **[복사]/[다운로드]**로 가져가세요.

- 결과는 자동으로도 저장됩니다: `works/<작품>/output/<회차>/final.txt`
- 한 번에 여러 회차를 고르면 **순서대로** 처리됩니다. (대량 일괄은 일부러 막아둠 — 회차를 골라서 진행)

---

## 3. 프롬프트 만들기·고치기 (프롬프트 작업자)

**[프롬프트 관리]** 탭. "번들(프롬프트 세트) = 한 작품·언어·버전의 STEP1~5 프롬프트 한 벌".

### 불러오기 / 새로 만들기
- **작품·언어·버전** 선택 → **[불러오기]**
- 새 작품: **[+ 새 작품/언어]** → 작품명·언어쌍 입력 → **[빈 5단계 템플릿 만들기]**

### 각 단계 카드에서 편집
- **단계 이름**
- **① 입력 자료** — 보통 그대로 둠. 기본이 `원문 · 설정집 · 이전 단계 결과`(자동 연결).
  다르게 엮고 싶을 때만 `직접 고르기`로 펼쳐 조정
- **② 사용 모델** — 비우면 번역 화면의 기본 모델. (이 단계만 다른 모델 쓰고 싶을 때 입력)
- **③ 출력 처리** — 대부분 안 만짐
  - `<o>…</o> 안만 추출`: AI에게 답을 `<o>…</o>`로 감싸라고 시킨 단계(JSON 등)에서만 켬
  - `완성본(최종)`: 최종 번역물이 나오는 단계(보통 마지막)에만 체크
- **④ 프롬프트 내용** — AI에게 줄 지시문 (마크다운·JSON 등 형식 자유)

### 저장
- **[현재 버전 덮어쓰기]** — 같은 버전에 저장
- **[새 버전으로 저장 (v2)]** — 버전 올려 저장 (이전 버전 보존)
- **[이 버전 삭제]** — 선택한 버전 삭제

> 번역 탭은 버전 미지정 시 **자동으로 최신 버전**을 사용합니다.

---

## 4. 프롬프트 개선하기 — 시험 실행(튜닝)

**[프롬프트 관리]** 탭의 **🧪 시험 실행** 바. *저장하지 않은 편집본*으로 바로 테스트합니다.

1. **샘플 회차** 선택 + **모델** 선택(기본 모의 = 키 없이)
2. **▶ 시험 실행 (전체)** → 각 STEP 카드 아래에 **그 단계 결과**가 표시됨
3. 결과가 약한 단계의 **④ 프롬프트 내용**을 수정
4. 그 단계의 **[↻ 이 단계부터 다시]** → 그 단계만 다시 실행(앞 결과 재사용 → 빠르고 저렴)
5. **[실제 보낸 프롬프트 보기]**로 입력 자료가 합쳐진 최종 프롬프트 확인 가능
6. 만족하면 **[새 버전으로 저장]** → 번역 탭이 자동으로 새 버전 사용

→ "결과 보고 → 프롬프트 고치고 → 다시 실행"을 반복하며 MT 품질을 끌어올리는 루프입니다.

---

## 5. 자주 묻는 것

- **키가 화면에서 빨간 '미설정'으로 떠요** → 그 모델용 키를 저장하거나, `🧪 모의` 모델로 바꾸세요.
- **회차 목록에 엉뚱한 파일이 떠요** → 폴더에 원문이 아닌 파일(프롬프트 txt 등)이 섞이면 같이 보입니다. 회차만 따로 폴더에 모으면 깔끔합니다.
- **TB 시트가 적게 떠요** → 화면은 *업로드한 파일에 실제로 있는 시트*만 보여줍니다. 시트를 추가하려면 엑셀에서 추가 후 다시 업로드.
- **결과 파일 위치** → `works/<작품>/output/<회차>/final.txt`
- **로그(앱 실행 문제 시)** → `/tmp/mtpe-app.log`

---

## 폴더 구조 (참고)

```
prompts/<작품>/<언어>/<버전>/   프롬프트 세트 (step1~5.txt + meta.yaml)
works/<작품>/
   episodes/   업로드한 회차 원문
   tb/         설정집 1개
   library.json  폴더경로·시트선택
   output/<회차>/final.txt   번역 결과
.env            API 키 (로컬)
MTPE.app        더블클릭 실행 앱
```
