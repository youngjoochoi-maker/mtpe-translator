# MTPE QC 진행 프로세스 (런북)

> 목적: 프로덕트가 이상 없이 작동하는지 **순서대로** 검증한다.
> 원칙: ① **모의 모드(비용 0)로 전체 흐름 먼저** → ② **Gemini로 실제 품질** → ③ 안정성·영속성.
> 막히면: 그 **화면을 캡처**하고 어느 Round/단계인지 적어 전달. (세부 항목 체크는 `QC체크리스트.md` 병행)

판정 표기: 각 Round 끝의 **[통과기준]**을 만족하면 ✅, 아니면 ❌ + 메모.

---

## Round 0 — 준비 (필수 선행)
1. **앱 실행**: `MTPE.app` 더블클릭 → 화면(앱 창/브라우저)이 뜨는지
2. **Gemini 키 준비**: https://aistudio.google.com/apikey → `AIza…` 키 발급
3. **키 저장**: 번역 탭 → 모델 `Gemini 2.5 Pro` 선택 → 키 칸에 `AIza…` 붙여넣기 → 저장 → **"설정됨 ✅"** 확인
   - ℹ️ **맥 현재 환경엔 이미 Gemini 키가 저장돼 있습니다**(`설정됨 ✅`로 표시되면 이 단계 생략 가능). 윈도우/새 PC에선 새로 저장 필요.
4. **샘플 준비**: 회차 원문 1~2개(폴더 지정 또는 업로드) + 설정집(TB) 1개 업로드
   - 평가표(Round 2.5)까지 보려면 **ko-en/ko-ja 작품**(예: `한영데모`·`샘플`)을 쓰세요.

**[통과기준]** 앱이 뜨고, Gemini 키가 "설정됨"이며, 회차·TB가 목록에 보인다.

---

## Round 1 — 모의 모드 스모크 테스트 (UI 전체 흐름, 비용 0)
> 키 쓰기 전에 UI가 다 도는지 빠르게 확인. 여기서 막히면 실제 번역 전에 잡는다.
1. 모델을 **🧪 키 없이 테스트(모의)** 로 변경
2. **번역 탭**: 회차 1개 선택 → 번역 → STEP1~5 진행(초록불) → 결과 표시
3. **결과 복사 / 다운로드** 버튼 동작 확인
4. **프롬프트 관리 탭**: 작품/언어/버전 **불러오기** → 5단계 프롬프트 표시
5. **시험 실행(모의)**: 샘플 회차 선택 → 시험 실행 → 각 단계 카드에 결과 표시

**[통과기준]** 모의 모드로 번역·다운로드·프롬프트 불러오기·시험 실행이 에러 없이 흐른다.

---

## Round 2 — 실제 번역 (Gemini) ⭐핵심 품질
1. 모델을 **Gemini 2.5 Pro** 로 변경
2. 회차 **1개** 선택 → 번역
3. 단계별 확인:
   - **STEP1**: `<o>…</o>` JSON 추출이 깔끔히 나오는지
   - **STEP3 초벌 / STEP5 최종**: 자연스러운 **한국어 번역**인지
   - **TB 반영**: 설정집의 인물명/용어가 번역에 쓰였는지
4. **누락/잘림** 없이 원문 끝까지 번역됐는지
5. **저장 확인**: `works/<작품>/output/<회차>/final.txt` 생성

**[통과기준]** Gemini로 5단계 실제 번역이 완료되고, 최종 결과가 납품 가능한 한국어이며, TB 용어가 반영된다.

---

## Round 2.5 — 평가표(MT 평가 시트) 생성 ⭐핵심 (ko-en/ko-ja 작품만)
> 번역 결과를 v2 평가표(.xlsx)로 자동 기입. **모의로 흐름 먼저 → Gemini로 품질** 순서 권장.
1. ko-en/ko-ja 작품(예: `한영데모`·`샘플`)으로 회차 번역 완료 상태에서, 최종 결과 영역의 **평가표 모드** 선택
2. **[평가 시트 생성]** → .xlsx 생성 → **다운로드/폴더 열기**
3. 엑셀 열어 확인:
   - **③ 평가 입력**: 원문·MT본 채워짐 + 집계 수식·드롭다운 **보존**
   - **④ 정성 평가**: 장면 ID·설명 / **⑤ 대시보드**: 버전·일자 / **⑥**: 현재 버전
   - **⑧ 평가자 일치율**: **장면별 샘플**(원문·MT본) 채워짐, 채점칸은 비고 일치 수식 보존
4. (Gemini) 장면이 여러 개로 나뉘어 ⑧이 고르게 채워지는지 / (모의) 2~3개만 차도 정상
5. 평가표는 **단일 파일 1개**만 생성됨(2명 이상 평가는 운영측이 복제·파일명 수정해 배포)

**[통과기준]** ko-en/ko-ja 회차에서 평가표가 생성되고, ③④⑤⑥⑧ 데이터가 채워지며 수식이 깨지지 않는다.
(zh-ko 작품은 평가표 버튼이 안 보이는 게 정상.)

---

## Round 3 — 프롬프트 개선 루프 (튜닝) ⭐핵심
1. 프롬프트 관리 탭 → **시험 실행(Gemini)** → 각 단계 결과 확인
2. 약한 단계의 **프롬프트 수정** → **[↻ 이 단계부터 다시]** → 수정이 결과에 반영되는지
3. **[실제 보낸 프롬프트 보기]** 로 입력자료가 합쳐진 프롬프트 확인
4. 만족 시 **[새 버전으로 저장(v+1)]** → 번역 탭에서 새 버전이 최신으로 잡히는지

**[통과기준]** 결과를 보며 프롬프트를 고치고 재실행해 개선이 반영되고, 새 버전이 저장·적용된다.

---

## Round 4 — 다중 회차 / 운영
1. 회차 **2~3개** 선택 → 번역 → **순차**로 처리되고 회차별 결과가 각각 나오는지
2. 각 회차 결과 **다운로드** 확인
3. (선택) TB **시트 선택** 바꿔서 → 결과에 반영되는지

**[통과기준]** 여러 회차가 순서대로 처리되고 결과가 회차별로 보관/다운로드된다.

---

## Round 5 — 안정성 / 엣지 (앱이 죽지 않는지)
1. **잘못된 키**(혹은 빈 키)로 실행 → **명확한 에러 메시지** + 앱 정상(안 꺼짐)
2. **회차 미선택** 상태로 번역 → 안내 메시지
3. **매우 긴 원문** 1개 → 끝까지 처리되는지(또는 한도 초과 시 메시지)
4. 번역 도중 **탭 이동/재실행** → 깨지지 않는지

**[통과기준]** 오류 상황에서 앱이 죽지 않고, 사람이 이해할 메시지가 나온다.

---

## Round 6 — 영속성 (재시작 후 유지)
1. 앱 **완전 종료 후 재실행**
2. 확인: **프롬프트 편집본 / 회차 목록 / TB·시트선택 / 결과(final.txt)** 가 그대로 유지

**[통과기준]** 재시작 후에도 작업물·설정이 보존된다.

---

## Round 7 — 윈도우 (선택)
1. 윈도우에서 `MTPE.exe` 실행 → 앱이 뜨는지
2. Round 1~2를 윈도우에서 간단 반복 (모의 → 실제 1회차)

**[통과기준]** 윈도우에서도 맥과 동일하게 동작한다.

---

## 최종 판정
| Round | 결과(✅/❌) | 메모 |
|---|---|---|
| 0 준비 | | |
| 1 모의 스모크 | | |
| 2 실제 번역 | | |
| 2.5 평가표 | | |
| 3 튜닝 | | |
| 4 다중 회차 | | |
| 5 안정성 | | |
| 6 영속성 | | |
| 7 윈도우 | | |

- **출시 가능 판단**: Round 0~3, 2.5, 5, 6 이 모두 ✅ 이면 핵심 기능 정상.
- 발견 이슈는 `QC체크리스트.md` 하단 "발견 이슈" 표에 정리 → 전달.
