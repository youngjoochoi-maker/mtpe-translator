# MTPE 서버 배포 가이드 (사내 다수 · 공용 키 · 비밀번호 로그인)

로컬 데스크톱 앱을 **사내 누구나 브라우저로 접속**하는 웹 서비스로 올리는 방법입니다.
구성: **공용 비밀번호 로그인 + 회사 공용 API 키(서버 보관) + 영구 디스크**.

---

## 0. 준비된 것 (코드에 이미 반영됨)

- `Dockerfile`, `.dockerignore` — 컨테이너 이미지
- **로그인 게이트** — `MTPE_APP_PASSWORD` 설정 시 활성화
- **공용 키 모드** — `MTPE_KEY_MANAGED=1` 설정 시 키 입력 UI 숨김, 서버 env 의 키 사용
- **영구 데이터** — `MTPE_DATA_DIR` 경로에 prompts/works 저장(없으면 기본 프롬프트 자동 시드)

### 서버용 환경변수
| 변수 | 값 | 설명 |
|---|---|---|
| `MTPE_APP_PASSWORD` | (사내 공유 비밀번호) | 로그인 게이트. **반드시 설정** |
| `MTPE_SECRET` | (긴 랜덤 문자열) | 세션 쿠키 서명용. 아무 긴 문자열 |
| `MTPE_KEY_MANAGED` | `1` | 키 입력 UI 숨김(공용 키 운영) |
| `ANTHROPIC_API_KEY` | (회사 Claude 키) | 실제 번역에 쓰는 공용 키 |
| `MTPE_DATA_DIR` | `/data` | 영구 디스크 경로 (Dockerfile 기본값) |
| `PORT` | (호스팅이 자동 주입) | 신경 안 써도 됨 |

> 다른 모델(OpenAI/Gemini)을 쓰면 `OPENAI_API_KEY` / `GEMINI_API_KEY` 도 추가.

---

## 1. 추천: Render 로 배포 (가장 쉬움)

1. **코드를 GitHub 저장소에 올립니다.** (private 권장)
   ```bash
   git init && git add -A && git commit -m "MTPE server"
   git remote add origin <회사 GitHub 저장소>
   git push -u origin main
   ```
2. [render.com](https://render.com) → **New → Web Service** → 그 저장소 연결
3. 설정:
   - **Environment**: Docker (Dockerfile 자동 인식)
   - **Instance Type**: 우선 가장 작은 것으로 시작 가능
4. **Disks** → **Add Disk**: Name `data`, **Mount Path `/data`**, 크기 1GB~ (프롬프트·회차·결과 보관용)
5. **Environment** 에 위 표의 변수 추가:
   - `MTPE_APP_PASSWORD`, `MTPE_SECRET`, `MTPE_KEY_MANAGED=1`, `ANTHROPIC_API_KEY`
6. **Create Web Service** → 빌드·배포 완료되면 `https://<이름>.onrender.com` 주소가 나옵니다.
7. 접속 → **비밀번호 입력** → 사용. HTTPS·도메인은 Render 가 자동 제공.

### 직원 안내
- 주소: `https://<이름>.onrender.com`
- 비밀번호: (사내 공유한 값)
- 키 입력 불필요(회사 키로 운영). 회차 올리고 번역하면 됨.

---

## 2. 대안: 클라우드 VM (직접 통제)

AWS Lightsail / EC2 등 리눅스 VM에서:
```bash
# Docker 설치 후
docker build -t mtpe .
docker run -d --name mtpe -p 80:8000 \
  -v /srv/mtpe-data:/data \
  -e MTPE_APP_PASSWORD="사내비번" \
  -e MTPE_SECRET="긴랜덤문자열" \
  -e MTPE_KEY_MANAGED=1 \
  -e ANTHROPIC_API_KEY="회사키" \
  mtpe
```
- HTTPS 가 필요하면 앞단에 nginx + Let's Encrypt(certbot) 를 둡니다.
- `-v /srv/mtpe-data:/data` 로 데이터를 호스트에 영구 보관.

---

## 3. 로컬에서 서버 모드 미리 보기

배포 전에 로컬에서 서버 모드를 그대로 켜볼 수 있습니다:
```bash
MTPE_APP_PASSWORD="test1234" MTPE_KEY_MANAGED=1 MTPE_SECRET="local-secret" \
ANTHROPIC_API_KEY="sk-ant-..." MTPE_DATA_DIR="$PWD/_serverdata" \
./.venv/bin/python -m mtpe.server
# → http://localhost:8000 접속 시 로그인 화면이 뜨면 정상
```

---

## 4. 운영 메모

- **백업**: `/data`(prompts/works/output) 를 주기적으로 백업하면 프롬프트·결과 보존.
- **비용 통제**: 공용 키라 로그인한 사람은 모두 회사 비용을 씁니다. 비밀번호 관리에 주의.
- **다음 단계(Phase 2)**: 사용자 늘면 작업 큐(끊겨도 유지·재시도), 사용량·비용 로깅, 계정별 권한을 추가하면 좋습니다.
- 데스크톱 앱(`MTPE.app`)은 **로컬 전용**입니다. 서버를 쓰면 직원들은 앱 설치 없이 URL 로만 접속합니다.
