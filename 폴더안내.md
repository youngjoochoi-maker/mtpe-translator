# 📁 폴더 안내 (쉬운 설명)

이 프로젝트에는 프로그램이 **두 개** 들어 있습니다.

- **MT추출** = 원문을 번역해 주는 프로그램 (`mtpe/` 폴더가 본체)
- **분권** = 소설을 권 단위로 나눠 주는 프로그램 (`novel_splitter/` 폴더가 본체)

두 프로그램이 **같이 쓰는 자료 폴더**가 두 개 있는데, 평소에 열어볼 곳은 사실상 이 둘뿐입니다.

---

## ⭐ 평소에 여기만 보면 됩니다

### 📂 `prompts/` — 번역 지시문(프롬프트)을 **불러오는** 곳
프로그램은 번역할 때 이 폴더에서 지시문을 읽어옵니다. 지시문을 **고치거나 새로 추가**하려면 여기만 보면 됩니다.

```
prompts/<작품>/<언어쌍>/<버전>/
        step1.txt ~ step5.txt   ← 단계별 번역 지시문 (실제 내용)
        meta.yaml               ← 이 작품의 설정표 (언어·단계 순서 등)
```
예) `prompts/필연/zh-ko/v1/step1.txt` = "필연" 작품, 중국어→한국어, 1번 버전의 1단계 지시문
버전을 안 고르면 **숫자가 가장 큰 폴더(최신 버전)**를 자동으로 씁니다. 새 버전은 `v2/` 폴더를 하나 더 만들면 됩니다.

### 📂 `works/` — 실제 작업물이 **저장되는** 곳
번역할 원문을 넣고, 결과가 쌓이는 곳입니다.

```
works/<작품>/
      episodes/  ← 번역할 원문 (넣는 곳)
      tb/        ← 설정집(TB) 파일
      output/    ← 번역 결과 (나오는 곳)
      eval/      ← 평가표
```
예) `works/샘플/output/1/final.txt` = "샘플" 작품 1화의 최종 번역 결과

> 참고: `prompts/`와 `works/`의 작품 이름은 서로 짝입니다. 지시문은 `prompts/`, 실제 원문·결과는 `works/`.

---

## 🗂 나머지 폴더 (가끔만 봄)

| 폴더 | 무엇인가 |
|---|---|
| `mtpe/` | **MT추출 프로그램 본체** (코드 — 건드리지 마세요) |
| `novel_splitter/` | **분권 프로그램 본체** (코드 — 건드리지 마세요) |
| `docs/` | 사람이 읽는 설명서 (사용법·배포·윈도우빌드 가이드 등) |
| `n8n/` | 자동화(n8n) 관련 자료 — 불러오기용 파일과 연동 가이드 |
| `qc/` | 품질검수(QC) 문서와 기록부 |
| `guides/` | 설치·운영 가이드(엑셀) 생성 자료 |
| `examples/` | 예시용 원문·설정집 샘플 |
| `tests/` | 프로그램 자동 점검용 (개발용) |

## ⚙️ 최상위의 낱개 파일들 (프로그램 "부품" — 옮기면 고장)

이 파일들은 **지금 위치·이름 그대로 있어야** 프로그램 실행과 윈도우 설치파일 만들기가 됩니다. **옮기거나 이름 바꾸지 마세요.**

- 실행 버튼: `run_api.py`, `run_app.py`, `run_splitter.py`, `run_splitter_api.py`, `MTPE-API서버.command`, `build_windows.bat`
- 포장 설정(윈도우 .exe 만들기): `mtpe.spec`, `mtpe_api.spec`, `novel_splitter.spec`, `novel_splitter_api.spec`
- 설정·기타: `config.yaml`(전역 설정), `.env`(비밀키), `requirements.txt`, `Dockerfile`, `MTPE.ico`

---

## 🔌 포트(창구 번호) 메모
같은 컴퓨터에서 두 프로그램의 자동화 서버를 동시에 켤 수 있도록 번호를 나눠 뒀습니다.
- **분권 API** = `8000`번
- **MT추출 API** = `8001`번
