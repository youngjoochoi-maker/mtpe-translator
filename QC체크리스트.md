# MTPE 프로덕트 QC 체크리스트

테스트 환경: ☐ 맥 앱(MTPE.app)  ☐ 윈도우(MTPE.exe)  ☐ 서버(브라우저)
테스트 모델: ☐ 모의(mock)  ☐ Gemini  ☐ Claude  ☐ 기타: ____
테스트 일자: __________  /  담당: __________

> 사용법: 각 항목을 직접 해보고 `[ ]` → `[x]`. 기대결과와 다르면 **맨 아래 "발견 이슈"**에 적어주세요.

---

## 0. 실행 / 접속
- [ ] 앱(또는 서버)이 정상 실행되고 UI가 뜬다
- [ ] 상단 **[번역] / [프롬프트 관리]** 탭 전환이 된다
- [ ] (윈도우) `MTPE.exe` 더블클릭 시 앱이 뜬다 (창 또는 브라우저)

## 1. 모델 & API 키
- [ ] 모델 드롭다운에 프리셋이 보인다 (Gemini/Claude/GPT/모의)
- [ ] **Gemini 선택 → 키 저장** → "설정됨 ✅"으로 바뀐다
- [ ] 키 없는 모델 선택 시 빨간 **"미설정 ❌"** 경고가 뜬다
- [ ] **🧪 모의 모델** 선택 시 키 없이도 경고 없이 진행된다
- [ ] (서버 공용키 모드일 때) 키 입력칸이 숨겨지고 "회사 공용 키" 안내가 뜬다

## 2. 설정집(TB)
- [ ] **양식 템플릿 다운로드(.xlsx)** 가 받아지고 열린다
- [ ] TB 파일 **업로드/교체** → 파일명이 초록색으로 표시
- [ ] **AI에 보낼 시트 선택** 체크박스가 그 파일의 실제 시트만 보여준다
- [ ] 시트 체크 해제/선택이 저장된다(재진입 시 유지)
- [ ] TB **삭제(✕)** 가 된다

## 3. 회차 원문 (라이브러리)
- [ ] **폴더 경로 지정 → [적용]** 하면 그 폴더의 회차가 목록에 뜬다
- [ ] **회차 개별 업로드** → 목록에 추가된다 (txt/docx 등)
- [ ] 업로드 회차는 **✕로 삭제**되고, 폴더 회차는 ✕가 없다(보호)
- [ ] **전체/해제**로 회차 일괄 선택이 된다
- [ ] 회차 선택 개수가 번역 버튼에 반영된다

## 4. 번역 실행 — 모의 모드
- [ ] 회차 1개 선택 → 번역 → STEP1~5가 **순서대로** 진행 표시(초록불)
- [ ] 각 단계에 "모의 응답" 결과가 뜬다
- [ ] 회차 **여러 개** 선택 → 순차로 처리되고 회차별 결과가 따로 나온다
- [ ] 최종 결과 **[복사] / [다운로드]** 가 동작한다 (앱에서 저장 다이얼로그)

## 5. 번역 실행 — 실제(Gemini) ⭐핵심
- [ ] Gemini로 회차 1개 **실제 번역** → 5단계 모두 실제 결과가 생성된다
- [ ] **STEP1** 결과가 JSON(`<o>…</o>` 추출) 형태로 깔끔히 나온다
- [ ] **STEP3 초벌 / STEP5 최종** 결과가 자연스러운 **한국어 번역**으로 나온다
- [ ] **TB 용어/인물명**이 번역에 반영된다 (예: 설정집의 국문명 사용)
- [ ] 원문 길이가 길어도 끝까지 처리된다 (누락/잘림 확인)
- [ ] 결과 파일이 저장된다: `works/<작품>/output/<회차>/final.txt`
- [ ] (품질) 번역 결과를 사람이 읽어 **납품 수준인지** 육안 평가

## 6. 프롬프트 관리 탭
- [ ] 작품/언어/버전 **불러오기** → STEP1~5 프롬프트가 채워진다
- [ ] 단계 **프롬프트 수정 → [현재 버전 덮어쓰기]** 저장된다
- [ ] **[새 버전으로 저장 (v+1)]** → 버전이 늘고, 번역 탭에서 최신으로 잡힌다
- [ ] **[이 버전 삭제]** 가 된다 (확인창 후)
- [ ] **[+ 새 작품/언어] → 빈 5단계 템플릿** 생성이 된다
- [ ] ① 입력 자료가 "원문·설정집·이전 단계 결과" 요약으로 보이고, **[직접 고르기]**로 펼쳐 조정된다

## 7. 프롬프트 튜닝 (시험 실행) ⭐핵심
- [ ] **샘플 회차 + 모델** 선택 후 **[시험 실행]** → 각 STEP 카드에 결과가 뜬다
- [ ] **Gemini로 시험 실행** 시 실제 결과가 단계별로 나온다
- [ ] 프롬프트를 고치고 **[↻ 이 단계부터 다시]** → 그 단계만 다시 돌고, 수정이 반영된다
- [ ] **[실제 보낸 프롬프트 보기]** 로 입력자료가 합쳐진 프롬프트가 보인다
- [ ] 튜닝 후 저장하면 번역 탭에 반영된다

## 8. 오류 처리 / 안정성
- [ ] 잘못된/만료된 키로 실행 시 **명확한 에러 메시지**가 뜬다(앱이 죽지 않음)
- [ ] 회차 미선택 시 안내 메시지
- [ ] 매우 긴 원문 / 빈 파일 등 이상 입력 시 동작 확인
- [ ] 번역 도중 다른 탭 이동/재실행해도 깨지지 않는다

## 9. 데이터 유지(영속성)
- [ ] 앱을 껐다 켜도 **프롬프트 편집본**이 유지된다
- [ ] 회차 목록 / TB / 시트 선택이 재시작 후 유지된다
- [ ] 결과(final.txt)가 보관된다

## 10. 평가표(MT 평가 시트) 자동 생성 ⭐핵심 (ko-en/ko-ja 작품)
- [ ] 번역 완료 후 최종 결과 영역에 **평가표 모드 선택 + [평가 시트 생성]** 버튼이 보인다
- [ ] (zh-ko 작품) 평가표 버튼이 **안 보인다**(템플릿 없음 — 정상)
- [ ] **[평가 시트 생성]** → .xlsx가 생성되고 **다운로드/폴더 열기**가 된다
- [ ] **③ 평가 입력**: 원문·MT본이 채워지고, 카테고리 드롭다운·집계 수식이 **그대로 보존**된다
- [ ] **④ 정성 평가**: 장면 ID·설명이 들어간다
- [ ] **⑤ 대시보드**(버전·일자) / **⑥ 버전 비교**(현재 버전) 표기
- [ ] **⑧ 평가자 일치율**: 원문/MT본이 **장면별 샘플**로 채워지고, 평가자 채점칸은 비어 있으며 일치 수식이 보존된다
- [ ] (모의) ⑧이 2~3문장만 차도 정상 / **(Gemini)** 장면이 여러 개로 나뉘어 더 많이 채워진다
- [ ] **A·B 비교 모드** 선택 시 A용/B용/최종본 **3부**가 생성된다
- [ ] 저장 위치 확인: `works/<작품>/eval/<회차>/…평가표.xlsx`

---

## 발견 이슈 (작성)
| # | 화면/단계 | 문제 내용 | 재현 방법 | 심각도(상/중/하) |
|---|---|---|---|---|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |

## 종합 의견
-
