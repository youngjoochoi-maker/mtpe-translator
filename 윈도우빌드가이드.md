# MTPE 윈도우 실행파일(.exe) 만들기

윈도우용 `MTPE.exe`(단일 실행파일)를 만드는 방법입니다.
> ⚠️ 윈도우 실행파일은 **윈도우에서 빌드**해야 합니다. (맥에서는 못 만듭니다)
> 윈도우 PC가 없어도 **방법 A(GitHub 자동 빌드)** 로 받을 수 있습니다.

---

## 방법 A — GitHub 가 자동으로 빌드 (윈도우 PC 불필요, 권장)

저장소(`youngjoochoi-maker/mtpe-translator`, private)에 이미 자동 빌드 워크플로가 들어 있습니다.
코드가 GitHub 에 올라가면 GitHub 가 **윈도우 서버에서 자동으로 `.exe` 를 빌드**해 다운로드하게 해줍니다.

### A-1. 가장 쉬운 방법 — Release 에서 .exe 받기 (권장)
- 버전 태그(`v1.1.0` 등)를 푸시하면 워크플로가 돌아 **Releases 에 `MTPE.exe` 가 자동 첨부**됩니다.
- 받는 사람: **Releases 페이지 → 최신 버전 → `MTPE.exe` 클릭 다운로드**
  👉 https://github.com/youngjoochoi-maker/mtpe-translator/releases/latest
- 빌드 거는 사람(관리자)이 새 버전 낼 때:
  ```bash
  git tag v1.1.0 && git push origin v1.1.0
  ```

### A-2. 수동 빌드 — Actions 아티팩트로 받기
1. 👉 https://github.com/youngjoochoi-maker/mtpe-translator/actions
2. 좌측 **Build Windows EXE** → 우측 **Run workflow** → (main 선택) → 초록 **Run workflow**
3. 몇 분 뒤 빌드 완료 → 그 실행 결과 화면 하단 **Artifacts** 의 **`MTPE-windows`** 를 다운로드
   - 압축(.zip)을 풀면 **`MTPE.exe`** 가 나옵니다. 이걸 윈도우에서 실행/배포하면 됩니다.

> 두 방법 모두 **GitHub 로그인 + 이 저장소 접근 권한**이 필요합니다(저장소가 private 이라서). 사내 인원에게 collaborator 로 초대하거나, 받은 `.exe` 파일만 사내 공유 드라이브로 전달하면 됩니다.
> 빌드가 실패하면 Actions 로그를 저에게 보여주세요 — 보통 누락된 모듈 한두 개를 `mtpe.spec` 에 추가하면 됩니다.

---

## 방법 B — 윈도우 PC 에서 직접 빌드

1. 윈도우에 **Python 3.11+** 설치 (설치 시 "Add to PATH" 체크)
2. 이 프로젝트 폴더를 윈도우로 복사
3. **`build_windows.bat` 더블클릭** (또는 명령창에서 실행)
4. 끝나면 **`dist\MTPE.exe`** 생성

---

## 실행 / 사용 (윈도우에서 받은 `MTPE.exe` 실행)

1. 받은 **`MTPE.exe`** 를 원하는 폴더(예: 바탕화면)에 둡니다. (설치 불필요)
2. **`MTPE.exe` 더블클릭** → 내부 서버가 뜨고 잠시 뒤 **앱 창**(또는 기본 브라우저)으로 UI 가 열립니다.
   - ⚠️ 처음엔 **"Windows의 PC 보호"(SmartScreen)** 경고가 뜰 수 있습니다 → **추가 정보 → 실행** (아래 문제 해결 참고)
   - 첫 실행은 단일 exe 를 임시폴더에 푸느라 **몇 초~십여 초** 걸립니다(정상).
3. 처음 실행 시 기본 프롬프트가 자동 준비됩니다.
4. **모델 선택 + API 키 입력** → "저장"  (키 없이 먼저 보려면 모델을 `🧪 키 없이 테스트(mock)` 로 선택)
5. 작품/언어/버전 선택 → 설정집(TB)·회차 원문 올리고 → 번역 실행 → 결과/평가표 받기

- **데이터 저장 위치 (윈도우)**: `%APPDATA%\MTPE\`  (탐색기 주소창에 `%APPDATA%\MTPE` 입력)
  - `prompts\` (프롬프트 편집본), `works\` (회차·결과·평가표), `.env` (API 키)
- **API 키**: 이 데스크톱 버전은 **각자 자기 키를 입력**해 씁니다(첫 화면에서 저장). 키는 그 PC 의 `%APPDATA%\MTPE\.env` 에만 저장됩니다.
  - 여러 명이 **공용 회사 키**로 쓰려면 데스크톱 배포보다 **서버 배포**(브라우저 접속)가 적합합니다 → `배포가이드.md`
- **종료**: 앱 창을 닫으면 종료됩니다(브라우저로 열린 경우 콘솔 창도 닫기).

---

## 참고 / 문제 해결

- **WebView2**: 앱 창은 윈도우의 Edge WebView2 런타임을 씁니다. Win10/11 엔 보통 이미 있습니다. 없거나 창이 안 뜨면 **자동으로 기본 브라우저로 열립니다**(정상 동작).
- **SmartScreen 경고**: 서명 안 된 exe 라 "Windows의 PC 보호" 경고가 뜰 수 있습니다 → **추가 정보 → 실행**. (사내 배포면 무방. 정식 배포는 코드서명 인증서 필요)
- **앱이 바로 꺼지면(디버깅)**: `mtpe.spec` 의 `console=False` 를 `console=True` 로 바꿔 다시 빌드하면, 검은 콘솔창에 오류가 보입니다. 그 메시지를 저에게 주세요.
- **용량**: 단일 exe 라 100~300MB 정도 될 수 있습니다(LLM 라이브러리 포함). 정상입니다.
- **첫 실행이 느림**: 단일 exe 는 임시폴더에 풀고 실행해서 처음 몇 초 걸립니다.

---

## 정리: 어느 걸 쓰나?
| 상황 | 추천 |
|---|---|
| 여러 명이 공용 키로, 설치 없이 | **서버 배포**(브라우저) — `배포가이드.md` |
| 개인 PC 에서 각자 자기 키로 로컬 사용 | **윈도우 .exe** — 이 문서 |
| 맥에서 로컬 사용 | **MTPE.app** (이미 있음) |
