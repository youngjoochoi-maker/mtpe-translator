# MTPE 윈도우 실행파일(.exe) 만들기

윈도우용 `MTPE.exe`(단일 실행파일)를 만드는 방법입니다.
> ⚠️ 윈도우 실행파일은 **윈도우에서 빌드**해야 합니다. (맥에서는 못 만듭니다)
> 윈도우 PC가 없어도 **방법 A(GitHub 자동 빌드)** 로 받을 수 있습니다.

---

## 방법 A — GitHub 가 자동으로 빌드 (윈도우 PC 불필요, 권장)

코드를 GitHub 에 올리면, GitHub 가 **윈도우 서버에서 자동으로 `.exe` 를 빌드**해 다운로드할 수 있게 해줍니다.

1. 코드를 GitHub 저장소에 push (private 권장)
   ```bash
   git init && git add -A && git commit -m "MTPE"
   git remote add origin <회사 GitHub 저장소>
   git push -u origin main
   ```
2. GitHub 저장소 → **Actions** 탭 → **Build Windows EXE** 워크플로 → **Run workflow** 클릭
3. 몇 분 뒤 빌드 완료 → 그 실행 결과 화면 하단 **Artifacts** 의 **`MTPE-windows`** 를 다운로드
   - 압축을 풀면 **`MTPE.exe`** 가 나옵니다. 이걸 윈도우에서 실행/배포하면 됩니다.
4. (선택) 버전 태그를 푸시하면 **Release** 에도 자동 첨부됩니다:
   ```bash
   git tag v0.1.0 && git push origin v0.1.0
   ```

> 빌드가 실패하면 Actions 로그를 저에게 보여주세요 — 보통 누락된 모듈 한두 개를 `mtpe.spec` 에 추가하면 됩니다.

---

## 방법 B — 윈도우 PC 에서 직접 빌드

1. 윈도우에 **Python 3.11+** 설치 (설치 시 "Add to PATH" 체크)
2. 이 프로젝트 폴더를 윈도우로 복사
3. **`build_windows.bat` 더블클릭** (또는 명령창에서 실행)
4. 끝나면 **`dist\MTPE.exe`** 생성

---

## 실행 / 사용

- **`MTPE.exe` 더블클릭** → 내부 서버가 뜨고 **앱 창**(또는 기본 브라우저)으로 UI 가 열립니다.
- 처음 실행 시 기본 프롬프트가 자동 준비됩니다.
- **데이터 저장 위치 (윈도우)**: `%APPDATA%\MTPE\`
  - `prompts\` (프롬프트 편집본), `works\` (회차·결과), `.env` (API 키)
- **API 키**: 이 데스크톱 버전은 **각자 자기 키를 입력**해 씁니다(첫 화면에서 저장). 키는 그 PC 의 `%APPDATA%\MTPE\.env` 에만 저장됩니다.
  - 여러 명이 **공용 회사 키**로 쓰려면 데스크톱 배포보다 **서버 배포**(브라우저 접속)가 적합합니다 → `배포가이드.md`

---

## 참고 / 문제 해결

- **WebView2**: 앱 창은 윈도우의 Edge WebView2 런타임을 씁니다. Win10/11 엔 보통 이미 있습니다. 없거나 창이 안 뜨면 **자동으로 기본 브라우저로 열립니다**(정상 동작).
- **SmartScreen 경고**: 서명 안 된 exe 라 "Windows의 PC 보호" 경고가 뜰 수 있습니다 → **추가 정보 → 실행**. (사내 배포면 무방. 정식 배포는 코드서명 인증서 필요)
- **앱이 바로 꺼지면(디버깅)**: `mtpe.spec` 의 `console=False` 를 `console=True` 로 바꿔 다시 빌드하면, 검은 콘솔창에 오류가 보입니다. 그 메시지를 저에게 주세요.
- **용량**: 단일 exe 라 100~300MB 정도 될 수 있습니다(LLM 라이브러리 포함). 정상입니다.
- **첫 실행이 느림**: 단일 exe 는 임시폴더에 풀고 실행해서 처음 몇 초 걸립니다.

---

## 정리: 어느 걸 쓰나?
| 상황 | 추천 |
|---|---|
| 여러 명이 공용 키로, 설치 없이 | **서버 배포**(브라우저) — `배포가이드.md` |
| 개인 PC 에서 각자 자기 키로 로컬 사용 | **윈도우 .exe** — 이 문서 |
| 맥에서 로컬 사용 | **MTPE.app** (이미 있음) |
